# F-4: Schema Parsing Library — Tasks

- [ ] Create `packages/collab/src/lib/types.ts` with all interfaces
- [ ] Implement `readProject()` and `readAllProjects()` in parsers.ts
- [ ] Implement `readContributors()` in parsers.ts
- [ ] Implement `readJournal()` with markdown section splitting
- [ ] Implement `readRegistry()` with markdown table parsing
- [ ] Write unit tests with inline fixtures
- [ ] Write integration test against real pai-collab repo
- [ ] Verify all tests pass
